#include<stdio.h>
#include<stdlib.h>
#include<time.h>
#include<math.h>

int main()
{
	double i = 9.6;
	//int result = exp(i); //e^9
	//double result = log(4) / log(2); //defaul base is e log_a_b = log_e_a / log_e_b
	//double result = sqrt(i); // 3
	//pow(x, y)
	//double v0 = floor(i); //return the largest integral value that is not greater than its argument
	//double v1 = ceil(i); //return the smallest integral value that is not less than its argument
	//double v2 = fabs(-i);
	//double v3 = fmod(i, 2);
	
	//000 000 000 billion million thousand
	//int loopNum = 700000000; //2,147,483,647 -> 3s 1s -> 700 000 000
	//while (loopNum--);
	//
	//clock_t clockTime = clock(); //return the number of clocks since program started
	//double second = clockTime / CLOCKS_PER_SEC;

	time_t timeValue;
	time_t time(timeValue);//return the number of seconds that have elapsed since 00:00:00 January 1, 1970


	return 0;
}