#include<stdio.h>
#include<assert.h>
#include<stdlib.h>

#pragma warning(disable: 4996)
/*
void abort(void);
void atexit( void (func)(void) );
void exit(int status);
*/

int main()
{
//	int* ptr = nullptr;
//#define NDEBUG
//	assert(ptr != nullptr); //only debug version is work

	//char* env = getenv("path");

	system("notepad.exe");
	return 0;
}