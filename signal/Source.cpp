#include<stdio.h>
#include<signal.h>

/*
int raise(int sig);
void (*signal(int si, void (*handler)(int)))(int);

//print variable argument list
//stdarg.h
int vprintf(char const *format, va_list arg);
int vfprintf(FILE *stream, char const *format, va_list arg);
int vsprintf(char *buffer, FILE *stream, char const *format, va_list arg);

*/

int main()
{
	return 0;
}